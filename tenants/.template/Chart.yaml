# Template for per-tenant Chart.yaml — copied by orbit-operator per tenant
# orbit-operator fills in: name, description, version
apiVersion: v2
name: tenant-SLUG
description: "Orbit tenant SLUG — auto-generated by orbit-operator"
type: application
version: 0.1.0
dependencies:
  - name: tenant-app
    version: "0.1.0"
    repository: "https://github.com/lustresconsulting/orbit-tenants"
