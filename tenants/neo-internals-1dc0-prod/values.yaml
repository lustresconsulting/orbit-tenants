# Auto-generated for internal service namespace — infra scaffold only
# No app components — OpenProject deployed separately via Helm chart
# Tenant: neo-internals-1dc0-prod | Plan: starter

tenant:
  slug: neo-internals-1dc0-prod
  namespace: t-neo-internals-1dc0-prod
  resourcePrefix: t-neo-internals-1dc0-prod
  plan: starter
  orgId: 79f99a82-41f8-4d4d-8103-30589731e620

database:
  mode: orbit

hpa:
  enabled: false

pdb:
  enabled: false

postgres:
  enabled: true
  instances: 1
  storage:
    size: 5Gi
    storageClass: local-path
  existingSecret: tenant-db-credentials
  existingSecretKey: password
  postgresUser: tenant
  database: appdb
  version: "17"

valkey:
  enabled: false

secrets:
  enabled: false

ingress:
  enabled: false

customDomain:
  enabled: false
  hostname: ""

monitoring:
  enabled: true
  serviceMonitor:
    interval: 30s
    scrapeTimeout: 10s
    path: /metrics
    port: metrics
  grafanaDashboard:
    enabled: true
    labels:
      grafana_dashboard: "1"

components: []
