# Auto-generated by Orbit deployment service
# Tenant: sunnepah-oktradesystem | Plan: starter

tenant:
  slug: sunnepah-oktradesystem
  namespace: tenant-sunnepah-oktradesystem-prod
  plan: starter

app:
  enabled: true
  image:
    repository: ghcr.io/sunnepah/oktradesystem/backend
    tag: a6922e503ef9
    pullPolicy: IfNotPresent
  replicaCount: 2
  port: 8000
  env:
    - name: DATABASE_URL
      value: "postgresql://postgres@tenant-sunnepah-oktradesystem-pg-rw:5432/app"
    - name: REDIS_URL
      value: "redis://tenant-sunnepah-oktradesystem-valkey:6379/0"
  resources:
    requests:
      cpu: 500m
      memory: 512Mi
    limits:
      cpu: 2000m
      memory: 2Gi
  livenessProbe:
    httpGet:
      path: /health
      port: 8000
    initialDelaySeconds: 15
    periodSeconds: 30
  readinessProbe:
    httpGet:
      path: /health
      port: 8000
    initialDelaySeconds: 10
    periodSeconds: 10

postgres:
  enabled: true

valkey:
  enabled: true

monitoring:
  enabled: true
